# Repository Side Image Slot

This folder controls the image used for the homepage section about doing the science in the repository.

To change it, delete the current image and add one new `.png`, `.jpg`, `.jpeg`, `.webp`, or `.svg` file. The website will use the image in this folder automatically, regardless of filename.

Use one image only. Images that show data, notebooks, workflows, or reproducible analysis work best.
